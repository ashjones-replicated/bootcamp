apiVersion: v2
name: bootcamp
description: File sharing web application fronting the upload service
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: replicated
    repository: oci://registry.replicated.com/library
    version: 1.19.1
